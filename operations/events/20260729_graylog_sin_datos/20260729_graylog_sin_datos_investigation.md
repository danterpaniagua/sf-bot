# Investigation — 20260729_graylog_sin_datos

**Status:** closed (Ops side) — `_ops.md` written 2026-07-30, PM and CLOUD dev emails sent. Remaining open item (root cause of the logging-verbosity change / Logstash restart) formally handed off to the CLOUD dev team via `_email_dev.md`; not blocking closure on the Operations side. **New unconfirmed observation added below (2026-07-31, later session) — partial volume recovery noticed incidentally, not yet root-caused; does not reopen closure by itself.**

## Observation (2026-07-31, later session) — partial volume recovery, incidental to unrelated indexing-failure work

While working an unrelated indexing-failure incident on the same Graylog instance (`operations/events/20260730_graylog_indexing_failure`), noticed via the Graylog UI (no stream filter — global search, roughly equivalent to Business+Sales combined given Default Stream has historically carried 0 messages):
- **Last 24h (query executed `2026-07-31T20:00:44Z`): 3,322,494 total results** — ~138K/hr average, ~27% of the pre-cliff baseline (~12,099,202/day, ~504K/hr, measured `20260723_franchise-scaling-costs` 2026-07-27).
- **Last 1h (query executed `2026-07-31T20:03:16Z`): 378,662 total results** — ~75% of historical peak baseline (e.g. Business alone peaked ~667,074/hr pre-cliff). Much higher than the 24h blended average, implying the recovery is recent/ongoing rather than a small bump spread evenly across the day.

Both measurements were taken **before** an unrelated `23:00:54Z` restart of the same Logstash process (part of today's indexing-failure remediation, on the same host `smartfran-graylog-pro`) — so this recovery predates and is independent of that specific restart. However, the Logstash process on this host *did* restart multiple times earlier today (`14:40:26Z`, `14:44:02Z`, `15:01:19Z`, `19:28:01Z` — see the indexing-failure event's scripts file for detail) for unrelated reasons (patch deployment, OOM crash-loop). No causal link to the volume recovery has been established — could be coincidental (e.g. dev-side fix landing independently) or could mean a Logstash restart was always the actual remediation this ticket's original "current working theory" section anticipated needing, now inadvertently applied for a different reason.

**Follow-up (2026-07-31, same session) — precise recovery timestamp found, strong causal candidate identified.** Ran the same Views API hourly-pivot method as C21/C25 (global, no stream filter — search id `6a6d2a3834e7b9a985ea4f42`, job id `6a6d2a5234e7b9a985ea4f9c`), 24h window ending `2026-07-31T23:05:54Z`:

| Hour (ART) | Count |
|---|---|
| 07-30 20:00 – 07-31 10:00 | 168–3,782/hr (flat, matches the documented post-cliff trickle) |
| **07-31 11:00** | **119,269** ← recovery starts here |
| 07-31 12:00 | 338,185 |
| 07-31 13:00 | 424,499 |
| 07-31 14:00 | 370,906 |
| 07-31 15:00 | 446,550 |
| 07-31 16:00 | 432,431 |
| 07-31 17:00 | 400,060 |
| 07-31 18:00 | 389,610 |
| 07-31 19:00 | 378,662 |
| 07-31 20:00 (partial, ~6min) | 11,629 |

**Recovery began at 2026-07-31 11:00 ART = 14:00 UTC**, then sustained in the 338K-446K/hr range for the rest of the day — solidly within the pre-cliff peak range (Business alone peaked ~667,074/hr; this is the combined total across Business+Sales+Default). Not a gradual ramp — a sharp jump within a single hour, then sustained.

**Strong causal candidate:** this hour coincides almost exactly with the start of *this session's* unrelated work on `smartfran-graylog-pro` (`operations/events/20260730_graylog_indexing_failure`) — specifically the first Logstash restart of the day, at `2026-07-31T14:06:33Z` (11:06:33 ART, deploying the indexing-failure v1 patch), which falls inside the exact hour the jump begins. Working theory: whatever state Logstash's Event Hub consumer got stuck in around the original `2026-07-28T06:06:40Z` restart (per this ticket's `Current working theory` section, never confirmed as crash vs. clean) persisted for ~3 days and simply needed *a* restart to clear — not necessarily today's specific patch content, just the act of restarting the process. This would mean the original volume-collapse root cause was likely a stuck/degraded Logstash consumer state, not a dev-side verbosity change (the leading alternate theory in this ticket, never confirmed either way).

**Not fully confirmed** — coincidence in timing is strong circumstantial evidence, not proof. No experiment isolates "restart alone" from "this session's specific config changes" as the actual trigger, since both happened together. Given the fix already shipped (multiple restarts happened today for other reasons) and volume has recovered, there's no clean way to test this further without deliberately breaking things again — not recommended. Suggest treating this as a plausible closing explanation for this ticket rather than pursuing further isolation.

**Recommended next step:** update this ticket's `_ops.md` (`Causa raíz` and `Resuelto` fields) to reflect the resolved volume, if the user agrees this is sufficient to close it — not done automatically here since `_ops.md` was already sent to PM/Dev on 2026-07-30 and changing it has stakeholder-communication implications beyond a working file.

## Confirmed facts
- **Original report corrected (2026-07-29, this session):** not a total outage starting 02:00 on 29/07 as first stated — actual report is **reduced (not zero) message volume since 2026-07-28 02:00**, a full day earlier and a different severity than originally investigated. Everything above investigated under the 29/07-total-outage framing (VM state, App Service config, EF Core verbosity git history, Event Hub metrics for 28/07 22:00-29/07 18:00) stays valid as evidence but was answering a different question than the one now in scope.
- Independent same-pipeline volume baseline exists: `20260723_franchise-scaling-costs` ops-events (2026-07-23) recorded Sales+Business combined at **12,099,202 events/day, measured 2026-07-27** — one day before the reported decline start. Useful comparison point once current volume is confirmed per-stream.
- Still separately confirmed and unexplained: the **live, real-time Logstash→GELF delivery flatline** (C7/C17, static journal, 0 events/sec) found earlier this session. Not yet established whether this is the tail end of the same 28/07-onward decline (degrading further to zero) or an unrelated, more recent break layered on top.
- **Outage is still ongoing** as of this session (C7 vs. C17, ~2 min apart): identical `journal_size` (104761887 bytes) and identical `oldest_segment` timestamp (`2026-07-29T17:36:31.138Z`) down to the millisecond between the two calls, both `append_events_per_second`/`read_events_per_second` = 0. Not a sampling artifact — zero messages have reached the GELF input across that entire interval. Contradicts the earlier "Graylog is working" read, which was apparently based on VM/UI reachability, not actual message flow.
- Graylog host is VM `smartfran-graylog-pro`, SmartIT Cloud subscription (`85c76dea-3304-4310-8656-bf21b28e4f4b`), eastus2, own VNet/NSG/disks (`cloud/docs/infrastructure.md`).
- Graylog VM resource group confirmed: `SMARTFRAN.CLOUD.PRO` (same shared-services RG as the App Services, not a dedicated RG despite "own VNet/NSG/disks" wording) (C0, this session).
- Stream IDs confirmed (C12): `PROD-Business-AppServicePlan` = `6a452a0d18ebc987b1ca003a`, `PROD-Sales-AppServicePlan` = `6a47c5c94b3c88a95fad7a7a`, Default Stream = `000000000000000000000001`.

## Resolved — scope confirmed
User confirmed (2026-07-29, this session): this is the right instance (SmartCloud/Business+Sales, `smartfran-graylog-pro`). The 02:00 timing in the original report was imprecise — the real, confirmed problem is the current Logstash→Graylog GELF delivery break happening right now. "Wrong instance" theory ruled out; no longer pursuing the other Docker Graylog stack for this event.

## Contradiction found — needs resolving before proceeding
Azure Event Hub metrics (`smartfran-graylog-evhns-pro`, C18, this session) show **continuous, healthy, ~1:1 matched Incoming/Outgoing Messages from 2026-07-28 22:00Z straight through 2026-07-29 17:30Z — including exactly the 02:00-02:45Z window** (459/453, 829/824, 519/517, 598/595 msgs per 15-min bucket). Only the final, still-in-progress bucket (17:45Z) is 0, which is expected (incomplete window, matches C7/C17 timing). **This directly contradicts a Graylog outage starting at 02:00 for this pipeline** — the Event Hub never stopped receiving or delivering.
Combined with the confirmed-static journal (C7/C17): Event Hub→Logstash consumption looks healthy and continuous all day; Logstash→Graylog GELF delivery is broken *right now*, but nothing confirms it's been broken since 02:00 — that timing came only from the original user report. Two live possibilities: (a) the Logstash→Graylog GELF output stage broke recently, unrelated to 02:00, or (b) **the original 02:00 report may be about the *other* Graylog instance** — `cloud-graylog/CLAUDE.md` documents a separate Docker Graylog stack serving SmartPedidos + SmartLoyalty, distinct from this SmartCloud-dedicated one (Business+Sales). This investigation pivoted to the SmartCloud instance based on a user instruction to check `cloud/repo/` verbosity, not on confirmation that it's the affected instance.

## Breakthrough — precise drop timing and magnitude confirmed (C21/C22, Views API)
48h hourly message count (all 3 streams — Business+Sales+Default combined, `query_string:"*"`), ART local time (UTC-3):
- 2026-07-28 01:00: 593,837 · 02:00: 700,112 (still normal/high)
- **2026-07-28 03:00: 25,144** — 96% single-hour drop
- 2026-07-28 04:00 onward: 1,387 / 945 / 1,520 / 2,307... settling into a **sustained ~250-3,600/hr trickle**
- Still only 318/hr as of 2026-07-29 14:00 (most recent complete bucket) — **no recovery in 46+ hours**
- Total across the 48h window: 6,004,893 (for reference against the 12,099,202/day Sales+Business baseline from 2026-07-27 — this 48h total is barely half of a single normal day)

**Precise cliff time: 2026-07-28 03:00 ART = 2026-07-28 06:00 UTC.** All prior Activity Log / git-commit-timing checks in this investigation used the wrong window (they checked 28/07 22:00 UTC – 29/07 06:00 UTC, based on the original "29/07 02:00" report) — **none of them cover the actual 06:00 UTC cliff on 28/07**. Those checks need redoing against the correct window.

Magnitude (~99.7% drop, zero recovery over 46h) argues against a clean intentional logging fix — yesterday's investigation estimated a Warning-level EF Core change would cut volume 30-50%, not to a near-total trickle. Looks more like a broken/degraded pipeline stuck passing only a fraction of real traffic than a deliberate config change — reopens the "actual pipeline break" theory, now with a precise, actionable timestamp to correlate against.

## Second contradiction — pre-cliff volume doesn't match Event Hub at all (C22-C24, this session)
Activity Log for the correct window (2026-07-28 04:00-09:00 UTC, brackets the real cliff): **zero relevant events** — only unrelated `SmartFran-Cloud-Orders-Plan-PRO` autoscale/hosting-plan activity (Orders isn't even wired into this Graylog pipeline). No deploy, restart, or config change on Business/Sales or the Graylog VM.
Event Hub metrics (`smartfran-graylog-evhns-pro`), same window, 5-min granularity: Incoming/Outgoing Messages flat and low (~100-800 per 5-min bucket, roughly 1,500-4,000/hr) **both before and after** the 06:00 UTC cliff — no change at all at the cliff time. Compare against Graylog's own histogram for the same clock hours (01:00-03:00 ART = 04:00-06:00 UTC): 593,837 and 700,112 msgs/hour. **100-400x mismatch.**
Since Event Hub is the only documented ingestion path for Business+Sales into this Graylog, and it shows no disruption whatsoever, **the pre-cliff high volume did not come through this pipeline.** The GELF UDP input (C9) is global (`0.0.0.0:12201`) and open to any sender, not exclusively this Logstash instance — the huge pre-cliff volume, landing in Business/Sales/Default (the only 3 streams queried, per Graylog's auto-applied default filter), more plausibly came from an unidentified other source hitting the input directly (matching stream rules by field coincidence, or landing in Default) that **stopped sending at 03:00 ART**, unrelated to the Business/Sales EF Core verbosity theory pursued earlier. Needs per-stream breakdown (not yet done — prior query combined Business+Sales+Default) to identify which stream(s) actually carried the pre-cliff spike vs. which stayed flat and match the Event Hub-consistent low volume. Running 3-query search (one per stream, `filter: {type:"stream", id:...}` — format taken from what Graylog's own default-filter auto-generated in the prior response) to resolve this.

**Retracted (this session):** the "mystery other sender" theory above. Per-stream breakdown (C25) shows the high pre-cliff volume genuinely belongs to Business and Sales (correctly stream-tagged, not misrouted) — Default Stream carried 0 messages across the full 48h. The Event Hub-vs-Graylog count gap is better explained by `cloud-graylog/CLAUDE.md`'s own documented pipeline behavior: Azure Diagnostic Settings batch many log records per Event Hub message, and Logstash explicitly "splits multi-record batches" before forwarding — so Event Hub `IncomingMessages` counts batches, not individual log lines. A few thousand batches/hour unpacking into hundreds of thousands of Graylog documents is expected, not a contradiction. Should have considered this before calling it a contradiction — retracting rather than building further on it.

## Confirmed — synchronized Business+Sales collapse (C25, this session)
Both streams collapse at the **same hour**, by similar magnitude, with zero recorded infra-side trigger:
| Stream | 2026-07-28 02:00 ART | 2026-07-28 03:00 ART | Drop |
|---|---|---|---|
| Business | 667,074/hr | 22,488/hr | ~96.6% |
| Sales | 33,038/hr | 2,656/hr | ~92% |
| Default | 0 (entire 48h) | 0 | — |

Both apps trickle at similarly low, roughly-stable levels for the full 46+ hours since (Business ~100-1,900/hr, Sales ~100-1,500/hr), no partial recovery. A synchronized, same-hour, similar-magnitude drop across **two independent App Services** with no Activity Log trace on either (C22/C23) points toward a **shared cause** rather than two coincidental independent events — most likely a shared configuration source both apps read from, e.g. Azure App Configuration (`SmartFran-Cloud-Settings-PRO`, confirmed shared across services) rather than per-app `appsettings.json`/App Service settings (already ruled out per-app in C3/C4). Business's `appsettings.json` also has a static `TelemetryFilter.SamplingPercentageLogs: 0.1` block (10% sampling) — worth checking whether Sales has the same pattern and whether a shared sampling/logging key changed centrally. `az appconfig kv list`/`revision list` (C26/C27) failed on a data-plane RBAC gap (C29 confirmed); unblocked via access-key auth (C30-C33, connection string used locally, not persisted anywhere). **Full key listing (C33) contains zero Logging/Telemetry/Sampling/LogLevel keys** — entire store is Cosmos DB connection settings + Blob Storage multimedia config per tenant, plus one feature flag. **Ruled out: shared App Configuration key as the mechanism.**

## Reopened (2026-07-30, this session) — indexing failures found, may overturn the verbosity theory
Graylog UI "Message Errors" panel: Input errors 0, processing errors 0, **failed to index: 146,813**. This is a real, large number and directly threatens the "reframed" conclusion below — the per-stream document counts (C25) that "verbosity dropped" was based on only count messages that successfully became searchable documents. If a large share of Business/Sales traffic is still being generated at the old rate but failing to *index* since the cliff, the true incoming volume may not have dropped at all — it could just have gone silently invisible to every query we've run so far (all of which search indexed documents, not raw ingest). `cloud-graylog/CLAUDE.md` documents a near-identical prior failure mode (2026-07-02: Business batch payload exceeding OpenSearch/Lucene's 32,766-byte max term length, 60K+ failures before a fix) — this could be a recurrence or something new. Not established yet: whether these 146,813 failures are concentrated around the 06:00 UTC cliff (would flip the whole theory) or spread evenly/pre-date it (would leave the verbosity theory intact as a separate, coincidental issue). Getting failure timing and actual error reasons next.

**Resolved (C34-C36, this session):** failures since 06:00 UTC 28/07 = 62,876; failures since 24h *before* the cliff = **identical, 62,876** — zero failures in the day leading up to the cliff, all 62,876 recent ones start exactly at the cliff and continue through now (sample timestamps `2026-07-30T12:18:57`, i.e. actively ongoing). All sampled failures (n=25) are on `business__6`, `type: indexing`, identical error: `Document contains at least one immense term in field="event_original"` — OpenSearch/Lucene 32,766-byte max term length exceeded, actual sizes 131,736-131,792 bytes. **This is the exact same bug class as `cloud-graylog/CLAUDE.md`'s 2026-07-02 incident** (that fix capped `event_original` to a single record's own JSON instead of the whole batch — but a *single record* still hitting 131KB here means one raw Business log message itself is enormous, consistent with `EnableSensitiveDataLogging(true)` logging full parameter values). Remaining ~83,937 of the 146,813 total (146,813 - 62,876) predate 27/07 06:00 — old, likely already-known/stale failures unrelated to this incident, not investigated further.
**Working theory now:** two entangled things started at the same hour (06:00 UTC 28/07) — the volume collapse (batch-fill-ratio reasoning still holds, real drop in what apps generate) *and* a recurrence of the `event_original` oversized-term indexing bug. Given they align exactly, most likely a single trigger explains both — a Logstash pipeline change/restart on `smartfran-graylog-pro` around that hour (would explain a reverted fix causing indexing failures) is now the leading candidate, and would NOT show in Azure Activity Log (C22/C23 were clean) since in-VM service changes aren't ARM-level operations. This is exactly the gap host-level access (never yet obtained this session) would close. Also means: post-cliff Business document counts (C25) are themselves a further undercount (silently-dropped indexing failures never became searchable), though the failure rate (~1,164/hr average) is far too small to account for the ~96% volume drop on its own — the two are related in timing, not equivalent in magnitude.

## Reframed primary theory (2026-07-30, this session) — real verbosity drop, not a broken pipeline — STATUS UNCERTAIN, see reopened section above
Event Hub batch counts (C24) are flat and consistent **both before and after** the 06:00 UTC cliff — same cadence of batches throughout, no drop in batch *frequency*. What collapsed is batch *fill*: pre-cliff ~667K Business log lines/hour over a roughly constant batch count implies ~170-220 records/batch; post-cliff ~22K/hour over the same batch count implies ~6 records/batch. A pipeline break would show fewer/no batches, not thinner ones — this pattern instead matches the **apps themselves generating far fewer log lines per unit time**, i.e. a real, large verbosity reduction at the application level, synchronized across Business and Sales. Mechanism still unconfirmed (not App Service settings, not App Configuration, not visible in git-tracked `appsettings.json`) but the *effect* is now well-supported by two independent data sources (Graylog document counts + Event Hub batch-fill ratio) agreeing with each other rather than contradicting.
This also reframes the earlier "live zero-throughput" alarm (C7/C17): at the new, much lower post-cliff baseline (~100-1,000 msgs/hour, i.e. low single digits per minute), a 2-minute window with zero journal movement is statistically unremarkable, not necessarily a distinct ongoing break layered on top. Walking back the urgency on that finding — it was calibrated against the old (pre-cliff) high-volume baseline, which no longer applies.
**Confirmed closed (Graylog UI Nodes page, this session):** node `f24bc98f`/`sfcloud-monitoreo` fully healthy — CPU 0.43%, JVM heap 1.1/4.0GiB (26.6%, note: actual provisioned heap is 4GB, smaller than either the "pilot" ~12GB or "full fleet" ~32GB figures in `cloud-graylog/CLAUDE.md`'s Terraform sizing doc — worth a correction there separately, tangential to this incident), all buffers (Input/Process/Output) at 0%, journal 58.5MiB/5GB (1.14%). Live throughput reads `0 msg/s` in/out — consistent with the low post-cliff baseline, not a symptom. No open technical thread left on the Graylog/infra side.

## New theory (2026-07-29, this session) — volume drop may be benign
User hypothesis: the volume reduction since 28/07 02:00 may be dev disabling `Microsoft.EntityFrameworkCore` Debug-level logging in Business — exactly the fix recommended in `20260728_logging-verbosity-ef-core-cors` (EF Core debug tracing was 19/20 of Business's top-20 `resultDescription` values, ~33% of its volume). If so, "less msgs since 28/07 2am" isn't a bug — it's the intended effect of that fix landing, separate and unrelated to the currently-confirmed live zero-throughput stall (C7/C17). Not yet verified: couldn't confirm via App Service config (C3/C4 found no Logging keys there) or via git (appsettings.json gitignored since 2026-06-16) — needs verification directly from message content in Graylog itself (are EF Core debug messages still present in recent Business traffic, or did they disappear around 28/07 02:00).

## Current working theory
Outage is real and ongoing (confirmed C7 vs C17). VM confirmed running (C1) and Graylog/OpenSearch themselves show **no resource pathology** (C6-C9, C11): journal at ~2% of cap and completely static across a 2-min window, ES cluster yellow is normal single-node baseline, zero journal-too-full/ES-unavailable notifications ever. This rules out the "verbosity saturated Graylog's fixed disk/heap" theory from `20260723_franchise-scaling-costs` H11 — that risk does not appear to have materialized here; Graylog side is clean because **no traffic is reaching it at all**, not because it's struggling to keep up with traffic. Fault is **upstream of Graylog**: most likely the Logstash process (consumes Azure Event Hub, forwards via GELF UDP 12201, runs on the same VM per `cloud-graylog/CLAUDE.md`) or the Event Hub pipeline/checkpointing stalled since ~02:00 and never recovered. Not yet confirmed — needs Logstash process state (host access) or Azure Event Hub metrics (Azure Monitor), neither reachable via the Graylog REST API.

## Ruled out
- VM-level outage: `smartfran-graylog-pro` confirmed `VM running` / VM Agent `Ready` (C1, this session).
- Graylog/OpenSearch-side disk or heap saturation as the cause: journal only 104MB/5GB (~2%), 1 segment age ~2min, ES cluster status yellow is the expected single-node baseline (not red/critical), and `/api/system/notifications` (C8) shows zero entries related to journal/indexer health — only one unrelated `outdated_version` notice from 2026-07-06. Directly contradicts the "H11 architectural risk materialized" theory floated earlier this session.
- Recent app-level log-verbosity change as visible in `cloud/repo/SmartFran.Cloud` git history — no commit touching `Logging:LogLevel` or EF Core logging config lands near 2026-07-29 02:00. The one fleet-wide change on 2026-07-28 (commits `2e2cc74b9`, `ef40e0740`, `6ae629b6e`, `492915f3b`, `54c342b6c`, `739152a7c`, `46bbe9bc8`, `3039f95eb`) is a GitHub Actions version bump (`actions/checkout@v3→v4`, `setup-dotnet@v3→v4`, `azure/webapps-deploy@v2→v3`) across every service's `*.PRO.yml` — unrelated to logging.
- `EnableSensitiveDataLogging(true)` hardcoded unconditionally in Business/Catalog/Sales/Platform `Program.cs` (confirmed by `git log -L` blame on Business: commit `32385fef2`, 2023-10-25) — real finding (sensitive data logging always-on in prod, no dev-only guard) but pre-existing for ~2 years, not a trigger for this incident's timing.

## Key limitation found
`appsettings.json` (and `.Production`/`.Staging` variants) for Business and Catalog APIs have been **gitignored since commit `7678983ab` (2026-06-16)** — where actual `Logging:LogLevel` values live. Deploy workflows also explicitly `rm` `appsettings.json` from the publish package before `azure/webapps-deploy` runs (confirmed in `SmartFran.Cloud.Business.PRO.yml` diff). So: **production logging verbosity is not visible in this repo at all** — it's sourced from Azure App Service Configuration/App Settings (or Azure App Configuration, `SmartFran-Cloud-Settings-PRO`) at deploy/runtime, not from tracked source. Any verbosity change (recent or not) can only be confirmed by reading the live App Service config, not by grepping this clone.

- App Service-level cause on Business/Sales PRO, in the 2026-07-28 22:00–2026-07-29 06:00 window (C3–C5, this session): no `Logging`/`ASPNETCORE_ENVIRONMENT` keys exist in either app's Application Settings (verbosity isn't controlled there — likely App Configuration `SmartFran-Cloud-Settings-PRO` or a deployed `appsettings.Production.json` not visible via env vars), and `az monitor activity-log list` scoped to `SmartFran.Cloud.PRO` returned **zero** entries for either `Business-PRO` or `Sales-PRO` in that window — no deploy, restart, or config write recorded. Rules out an App Service-side trigger at the time of the outage; redirects focus back to the Graylog host itself.

## Key reference — separate project (`~/Documentos/git/cloud-graylog/`, not in this monorepo)
`smartfran-graylog-pro` is the **SmartCloud-dedicated** Graylog instance (Business + Sales only, via Event Hub/Logstash) — distinct from the older Docker Graylog serving SmartPedidos+SmartLoyalty. Confirmed details from `cloud-graylog/CLAUDE.md`:
- External URI: `https://sfcloud-monitoreo.smartfran.com/graylog/` (nginx reverse proxy, port 443; Graylog itself binds `127.0.0.1:9000`, OpenSearch `127.0.0.1:9200` — both internal-only).
- **Pre-existing architectural risk, flagged 2026-07-23 (`20260723_franchise-scaling-costs` H11, ops-events 2026-07-23):** unlike PaaS Log Analytics, this VM has **fixed disk and memory** — "un aumento de verbosidad no controlado... podría llenar el disco o saturar el heap de OpenSearch y detener la ingesta de logs" — flagged as an identified architectural risk (OpenSearch known behavior), not yet a reproduced failure at the time. Partial supporting evidence then: OpenSearch heap baseline 63-64%, serving only 2 of 8 App Services. **This incident looks like that risk materializing.**
- Sizing is provisioned per onboarding phase (`local.phase_config` in Terraform) — pilot (Sales only, D8s_v6, ~12GB heap) vs. full fleet (E16s_v6, ~32GB heap). Only Business+Sales are onboarded, so actual current VM size needs confirming — may still be pilot-sized, which would make headroom tighter than the "full fleet" numbers suggest.
- Access confirmed: REST API with token (this session). Base API path: `https://sfcloud-monitoreo.smartfran.com/graylog/api/`.

## Open questions / next steps (current, post-indexing-failure-discovery)
- Get host-level access to `smartfran-graylog-pro` via `az vm run-command` (never actually obtained this session despite being flagged repeatedly) — check Logstash's `event_original` handling in its current pipeline config (did the 2026-07-02 fix get reverted/changed?), and its restart/reload history around 2026-07-28 06:00 UTC (`systemctl show logstash --property=ActiveEnterTimestamp`, `journalctl -u logstash` for that window).
- If Logstash was restarted/redeployed at that hour, that's the likely single trigger for both the volume drop and the indexing-failure recurrence — would resolve the investigation without needing the dev-side verbosity question at all.

**Confirmed (C37/C38, this session): Logstash restarted at `2026-07-28 06:06:40 UTC`** — `ExecMainStartTimestamp`/`ActiveEnterTimestamp` both show this exact time, and the journal shows a genuine fresh pipeline init (`Starting pipeline {pipeline.workers=>8, pipeline.batch.size=>125, pipeline.batch.delay=>50, pipeline.max_inflight=>1000}`, config `/etc/logstash/conf.d/azure-eventhub-to-graylog.conf`, Event Hub consumer group re-established at `06:06:58`, resuming from a checkpointed offset). This is essentially simultaneous with the volume-cliff hour (03:00 ART / 06:00-06:59 UTC bucket).
**Retracting the app-side "dev changed logging" theory as primary** — a Logstash restart at almost exactly the cliff moment is a far more direct, better-timed explanation for both the volume collapse and the `event_original` indexing-failure recurrence than an independent, coincidentally-timed app config change. Two things now unconfirmed and left to resolve: (1) **why** Logstash restarted (crash/OOM, systemd auto-restart, unattended-upgrades/OS patching, a deploy/config push, or manual action — journalctl around 06:06:40 itself, just before the restart, would show a crash trace if that's the cause; not yet pulled), and (2) whether the *current* pipeline config still applies the 2026-07-02 `event_original` single-record fix, since the ongoing indexing failures suggest it may have reverted (C39, run but not yet reported back this session).

**C39 result: the fix IS present and correct** — `/etc/logstash/conf.d/azure-eventhub-to-graylog.conf` line 62: `event.set("event_original", record.to_json)`, explicitly commented as the 2026-07-02 fix, scoping `event_original` to one record's own JSON rather than the whole batch. **Retracting "reverted fix" as the indexing-failure cause.** Since the fix is correctly isolating to a single record and that single record is *still* 131KB+, the real cause is that **individual Business log messages themselves are enormous** — consistent with `EnableSensitiveDataLogging(true)` (Business `Program.cs`, confirmed earlier) logging full SQL parameter values, large enough to blow the 32,766-byte term limit on their own. This is a distinct, parallel issue from the volume-drop question, not unified with it by this piece of evidence — it's real, ongoing data loss (~1,164/hr) regardless of what caused the volume drop.
Still open: why Logstash restarted at `06:06:40 UTC`, so close to the cliff hour — worth one more check (journal in the minutes *before* the restart, for a crash trace vs. a clean stop/start) if closing that loop matters; otherwise this may be close to as far as infra-side investigation can take it, and the two remaining threads (why the restart happened; why individual Business records are oversized) are better handled as separate follow-up items than blocking the ticket.
- Separately, regardless of root cause: the ongoing `event_original` indexing failures are real, current data loss (~1,164/hr) and should be flagged/fixed on their own merit once root cause is confirmed.

## Open questions / next steps (current, post-breakthrough)
- Redo Activity Log check for `SmartFran.Cloud.PRO` (Business/Sales App Services) **and** the Graylog VM/Event Hub RG, scoped to `2026-07-28T05:00:00Z`-`2026-07-28T08:00:00Z` (brackets the real 06:00 UTC cliff) — the earlier C2/C5 checks covered the wrong day.
- Re-examine `cloud/repo/SmartFran.Cloud` commit `fc51a23ea` (Stg→PRO deploy, merged `2026-07-28T10:08:49Z` UTC) — about 4h *after* the cliff, so probably not the direct cause, but worth confirming no earlier deploy/hotfix landed closer to 06:00 UTC.
- Check Azure Event Hub metrics (C18 data) at finer resolution right around 06:00 UTC on 28/07 specifically — the 15-min buckets we already have (22:00 28/07 onward) don't cover 06:00 UTC either; need `--start-time 2026-07-28T05:00:00Z --end-time 2026-07-28T08:00:00Z`.
- Given the magnitude/no-recovery pattern, check Logstash directly (via `az vm run-command`, still not yet done) for a crash/restart loop or pipeline error starting around 06:00 UTC on 28/07 — this is now the most direct way to confirm root cause.

## Open questions / next steps (current)
- Check Logstash process/pipeline state on `smartfran-graylog-pro` via `az vm run-command` (systemctl status, recent journal/log errors on the GELF output stage specifically, since Event Hub input side is confirmed healthy).
- Once Logstash state is known: if it's running but silently failing on output, check its config for the GELF output target (host/port) and confirm nothing changed (e.g. Graylog GELF input still bound `0.0.0.0:12201`, confirmed still correct per C9).
- If Logstash itself looks fine, check local network path from Logstash to `127.0.0.1:12201` (NSG shouldn't matter, same host — but local firewall/iptables or a Graylog input crash/rebind could).
- User requested a 24h message count/histogram per stream to see the actual gap shape (not just the current-instant journal flatline). Classic `/api/search/universal/relative(/histogram)` confirmed **404 on this Graylog 7.1.3 instance** (C19, C20, this session) — removed in favor of the Views-only search API. Same token/headers worked fine on C6-C12, so this is a real API removal, not an auth issue.
- User then asked for a 48h (2-day) hourly message count via API specifically (not the UI). Attempted `/api/views/search` (create) + `/api/views/search/{id}/execute` (run). First schema guess failed silently (empty ID, cascaded 405). Second attempt: `row_groups[0].interval` (`TimeUnitInterval`) only accepts `type`/`timeunit` fields, not `value`/`unit`. Third attempt (`timeunit: "PT1H"`, ISO-8601): rejected too — Graylog wants its own shorthand, `{quantity}{unit}` with unit in `[smhdwMy]`, e.g. `"1h"`. Fourth attempt (interval fixed): rejected on a new field — pivot `search_types[0]` requires a `rollup` boolean, not included in the original guess. Retrying with `"rollup": true` added — **succeeded**, search created (`id: 6a6a3ddf34e7b9a985a04019`). Working schema for future reference: `{queries:[{id, query:{type:"elasticsearch",query_string:"*"}, timerange:{type:"relative",range:<seconds>}, search_types:[{id, type:"pivot", rollup:true, row_groups:[{type:"time", field:"timestamp", interval:{type:"timeunit", timeunit:"<N><s|m|h|d|w|M|y>"}}], series:[{type:"count", id:"count()"}]}]}]}`. Executed via `/api/views/search/{id}/execute` — async, returned `done: false` and a job id (`6a6a3e0034e7b9a985a0578c`, distinct from the search id). Needs polling; trying `/api/views/search/status/{jobId}`, unverified.

## Open questions / next steps (superseded — kept for history per non-append-only convention, remove once fully stale)
- Confirm VM power state and recent activity log entries around 2026-07-29 02:00.
- Confirm whether this is total silence (no inputs at all) or partial (specific input/source only).
- Confirm access method available (Azure CLI `az vm run-command`, SSH, or Graylog web UI/API) to check service and disk state on the host.
- Check disk usage on Graylog VM (journal fills disk → ingestion halts, no error dashboard) — worth checking regardless, since `20260728_logging-verbosity-ef-core-cors` already found EF Core debug-level tracing dominating Business's ingest volume as of yesterday; if that volume tipped the Graylog VM's disk/journal over the edge overnight, verbosity doesn't need to have *changed* today to be the trigger.
- If pursuing the verbosity angle further: pull `az webapp config appsettings list` for Business (`SmartFran-Cloud-Business-PRO`) / Sales (`SmartFran-Cloud-Sales-PRO`) App Services (`SmartFran.Cloud.PRO` RG, `cloud/docs/infrastructure.md`) and diff against `az monitor activity-log list` for any config write around/before 2026-07-29 02:00 — that's the only place a real verbosity change would show up.
